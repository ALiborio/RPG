using UnityEngine;
using System.Collections;

public class Pond : Block {

	// Use this for initialization
	void Start () {
		// Initialize resources
		addResource(new FreshWater(100,500);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
